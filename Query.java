import java.util.*;
import java.io.*;

class Query {

    private String[] words;

    /*void querydb(Database d){
      boolean run = true;
      while (run){
        Console console = System.console();
        String line = console.readLine("> ");
        words = line.split(" ");
        if (words[0].equals("select")) d.select(words);
        if (words[0].equals("print")) d.print_data();
        if (words[0].equals("save")) d.save_data(words[1]);
        if (words[0].equals("addcol")) d.addColumn(words);
        if (words[0].equals("delete")) d.delete(words);
        if (words[0].equals("load")) d.load(words);
        if (words[0].equals("quit")) run=false;
      }
    }*/
}
